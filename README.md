# @fw-systeme/node-periphery
GPIO and SPI libraries for the modern Node.js 24+ world.

## Installation

This package is published only on GitHub Packages.

1. Configure npm to use GitHub Packages for the `@fw-systeme` scope:

```ini
@fw-systeme:registry=https://npm.pkg.github.com
```

2. Authenticate against GitHub Packages (for private packages or rate-limited environments):

```ini
//npm.pkg.github.com/:_authToken=YOUR_GITHUB_TOKEN
```

3. Install the package:

```bash
npm install @fw-systeme/node-periphery
```

## Breaking Change

Releases are no longer published to npmjs.com. The package name changed from `node-periphery` to `@fw-systeme/node-periphery`.
